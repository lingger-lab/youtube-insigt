# ISSUES — 미해결 이슈 · 보류 설계

> "나중에 신경 쓰기로 한 것"을 잊지 않기 위한 목록. 기재일 2026-09-07.
> 해결되면 항목을 지우고, 판단이 바뀌면 이유를 적는다.

## A. 공개 전에 반드시

| # | 항목 | 내용 | 비용 |
|---|---|---|---|
| A1 | **할당량 증량 신청** | search.list 전용 버킷 하루 100회는 구매 불가. 유일한 경로는 [심사 기반 확장 신청](https://support.google.com/youtube/contact/yt_api_form). 검토에 수주~수개월 걸리므로 필요해질 때 시작하면 늦다. | 0원, 시간 |
| A2 | **Vercel Hobby 상업 이용 금지** | 공개(상업) 시 Pro $20/월. 이 앱의 유일한 금전 비용. | $20/월 |
| A3 | **Vercel env 교체만 남음** | 노출됐던 키는 이미 삭제된 상태였고(2026-09-08 확인, `API key not valid`), 병합 후 자동배포로 번들에서도 사라졌다. 전용 프로젝트 `youtube-insigt`에 YouTube API만 허용하는 서버 키를 새로 발급해 `.env.local`에 넣었다. **남은 것**: Vercel 대시보드에서 `NEXT_PUBLIC_YT_API_KEY` 삭제 → `YT_API_KEY`(새 키) 추가. 그 전까지 배포본 검색은 전부 실패. | 0 |
| A4 | **레이트리밋 — 인메모리 구현됨, 전역 상한은 아님** | `server/rateLimit.ts`: IP당 10분에 검색 10회, LLM 3회 (슬라이딩 윈도). **한계**: 서버리스 인스턴스마다 따로 세고 콜드 스타트에 초기화된다 → 한 클라이언트의 폭주는 막지만 전역 하루 100회 상한을 보장하지 못한다. 공개 트래픽이 생기면 Vercel KV/Upstash로 교체. | 0 (KV 전환 시 무료 티어) |
| A5 | **앱 내 LLM 분석 비용 상한** | A4의 IP당 10분 3회가 걸려 있으나 인스턴스 단위라 전역 예산 캡은 아니다. 공개 전 일일 예산 캡(KV 카운터 또는 Anthropic 콘솔 spend limit) 필요. 개인용이면 키를 넣지 않는 것도 선택지. | 호출당 ~$0.1~0.5 (Opus 5, 이미지 12장 기준 추정) |

## B. 지표·데이터 유효성 (알려진 결함)

| # | 항목 | 내용 |
|---|---|---|
| B1 | **포맷 분리 정규화 — 실측으로 효과 재평가 (2026-09-08)** | 구현됨. 다만 "실익이 컸다(3.6배 과소평가)"는 1차 보고는 **틀렸다**: 그 3.6배는 평균→중앙값, 전체생애→최근50편 효과가 대부분이었다. 같은 동료 집합에서 포맷만 분리한 2차 실측은 **중앙 효과 ≈ 0**(혼합중앙값÷같은포맷중앙값 p50 0.92~1.00), 꼬리에서만 교정(p10 0.44~0.60, p90 1.2~1.5; 교차 포맷 대상은 1.8~3배). 즉 원리적으로 옳고 소수 사례를 바로잡지만 체계적 교정은 아니다. 혼합 포맷 채널은 45~86%. |
| B2 | **2026-08-27 조회수 집계 기준 변경** | 모든 포맷에서 재생 시작 즉시(자동재생·호버 포함) 조회수 카운트. 이 날짜 전후 영상의 `viewCount`·`viewsPerDay`는 같은 기준이 아니다. 프롬프트 "없는 것" 목록에는 기재함. 지표 보정은 미적용. |
| B3 | **런타임 검증 — 주 경로 완료 (2026-09-08)** | 실키로 검색 50개×3 키워드, 200개×1, 브라우저 E2E 1회. 스키마 오류 0건, 200개 19.2초(채널 154개, 공용 316 units). **엣지 미검증**: 라이브·프리미어·연령제한·삭제 영상·업로드 0편 채널. |
| B4 | **강조 임계값 — 실측으로 교체 (2026-09-08)** | 절대 2배는 78~94%를 강조해 무의미했다(성과배수 p50이 키워드별 12~29배). 결과 집합 상위 20% **그리고** 절대 5배 이상으로 교체. 통계적 의미는 없는 표시 눈금이다. |
| B7 | **누적 배수는 오래된 대상에 유리하다** | 검색 결과 영상은 동료(최근 50편)보다 수년 오래됐다(대상 나이 p50 695~2247일 vs 동료 중앙값 148~761일; 대상보다 어린 동료 비율 p50 1.00). 30일 미만 동료를 빼는 "나이 맞춤"은 **효과 없음**(p50 1.00)이라 폐기. 대신 `viewsPerDayMultiple`(일평균 배수)을 나란히 둔다 — 현행 누적 배수가 이보다 p50 2.3~6.7배 크다. 어느 쪽도 단독 진실이 아니다. |
| B9 | **초보 사용자 단순화 미착수** | PRD의 "초보 사용자(UI 단순화)" 타깃과 달리 깊이 선택·대조군·자막·앱 내 분석·보관함·내 주제로 복잡해짐. 사용자 결정(2026-09-09): 시안 구조가 확정된 뒤 "기본/고급" 표기·접기를 검토. |
| B10 | **불꽃 애니메이션 제거** | 원본의 `animate-pulse` 강조를 뺐다(reduced-motion·주의 분산). 🔥 배지만 남김. 되살리려면 `motion-reduce:animate-none` 필수. |
| B11 | **대조군의 포맷 혼합** | 상위군 Shorts 9 / 하위군 롱폼 4처럼 섞이면 길이·구조 비교가 포맷 효과에 오염된다(V.5에서 모델이 지적). 표에 포맷 열·군 요약·"포맷 다른 행끼리 비교 금지"로 완화. 근본 해결은 포맷별 대조군(Shorts끼리/롱폼끼리) — 표본이 반으로 줄어 보류. |
| B12 | **영상 관찰의 편향·언어** | 20편 수집에서 `thumbnailPromise.kept`가 20/20 `yes` — 레시피 영상(썸네일=완성 요리)이라 자연스럽지만 긍정 편향 가능성. 다른 장르로 재확인 필요. 서술 언어가 6/20 영어로 흘러 지시문에 "한국어로" 추가(인용은 원문). |
| B8 | **`contentDetails.caption` 의미 미검증** | 실측 12~14%. 수동 업로드 자막만 잡고 자동 생성 자막은 안 잡는다고 알려져 있으나 OAuth 없이는 확인 불가. "자막 있음" 표시는 과소일 가능성이 높다. |
| B5 | ~~README 상단 서술 드리프트~~ **해소(2026-09-08)** | README·CLAUDE.md 구조 절, PRD 지표표·사용자 흐름, TRD 구조·API·환경변수, Tasks 계산식을 현행 코드(포맷별 중앙값, 라이브 제외, 상대 강조, 컨택트시트, 앱 내 LLM)에 맞춰 갱신. 재발 방지 규칙: 문서는 코드와 같은 커밋에 싣는다. |
| B6 | **대조군은 검색 결과 안에서만** | relevance 50의 상위/하위 비교. YouTube가 걸러준 표본 안의 상대 비교이며 "검색에도 안 뜨는 실패작"은 여전히 안 보인다. 크리에이터 질문("이 니치에서 잘 되는 영상의 특징")에는 충분한 범위로 판단, 한계는 프롬프트에 명시. |

## C. 제품 제약 (해결 불가 — 숨기지 말 것)

- **타인 영상 자막**: `captions.download`는 소유자 OAuth 필수. 비공식 엔드포인트는 ToS §5.B 위반 소지. 공개 제품이면 **사용자 붙여넣기 입력란**만 안전.
- **CTR·노출수·시청 지속률**: 채널 소유자 전용(Analytics API).
- **알고리즘 노출량**: 조회수에서 분리 불가.
- **썸네일 이미지**: 클립보드 텍스트로는 안 넘어감. `i.ytimg.com`은 CORS 헤더가 없어 클라이언트 캔버스 합성도 불가 → 서버 프록시가 있어야 함.

## V. 키 확보 후 순차 검증 체크리스트 (2026-09-07 코드 선반영분)

아래는 **코드는 있으나 실제 API·브라우저로 검증하지 못한** 것들이다. 순서대로 진행하고,
통과한 항목은 지우고 실패한 항목은 B절로 옮긴다.

**0. 준비** — 완료 2026-09-08 (키: `campone-plane` 프로젝트의 기존 키, API 제한 적용)
- [x] `npm run measure` 3키워드 + `scripts/measure-isolate.ts` 3키워드. 출력 `measure-out/`

**1. 실측 결과 (2026-09-08, 검색 총 13회 소비)**
- [x] 성과배수 p50 12~29배, 2배 이상 78~94% → 절대 2배 기준 폐기, 상대 20% + 하한 5배로 교체 (B4)
- [x] 기준선 출처 `format-median` 44~49/50. 업로드 목록 없음 0%, 채널당 42~48편
- [x] 옛/새 기준선 3.6배 차이는 **포맷 효과가 아니었다** — 같은 동료로 분리하면 중앙 효과 0 (B1 재평가)
- [x] 혼합 포맷 채널 45~86% → 포맷 분리는 유지 (소수 사례 교정)
- [x] 구독자 비공개 0%, 좋아요 숨김 0~4%, maxres 96~100%, 자막 트랙 12~14%(B8)
- [x] 소요 시간 50개 3.7~6.8초, 200개 19.2초(채널 154, 공용 316 units)
- [x] 검색 호출 초과: 첫 페이지 46개 반환 + 페이지 간 중복으로 50개 검색에 4회 소비 → 호출 수를 페이지 수로 고정하고 결과가 적은 걸 받아들이도록 변경
- [x] 나이 맞춤 기준선(30일 이상 동료) 효과 없음(p50 1.00) → 폐기, 일평균 배수 병기 (B7)

**2. 검색 파이프라인** — 완료 2026-09-08
- [x] 실제 검색 → 렌더. `MALFORMED_RESPONSE` 0건 (150편 + 200편)
- [x] 툴팁 "같은 채널의 최근 … 중앙값 대비" 49/50, `*`(lifetime) 1/50
- [x] 200개: 19.2초, `otherUnits` 316 (추정 상한 408 이내)
- [ ] 엣지: 라이브·프리미어·연령제한·삭제 영상·업로드 0편 채널 — 미검증

**3. 썸네일 컨택트시트 (Chromium, Playwright)** — 완료 2026-09-08
- [x] `/api/thumbnail` → 200 image/jpeg 65KB, `X-Thumbnail-Variant: maxresdefault`
- [x] "썸네일 시트 복사" → 클립보드에 PNG 2.5MB, 20칸 `#1~#20` 라벨 렌더 확인(스크린샷)
- [x] 라벨 ↔ 시장 프롬프트 표 20행·썸네일 링크 20개 일치
- [x] 다운로드 폴백 — Chromium에서 `ClipboardItem`을 제거해 시뮬레이션: PNG 3.9MB 다운로드, 버튼 "이미지 내려받음". **실제 Safari/Firefox는 미설치로 미확인** (둘 다 2024년 이후 ClipboardItem 이미지 지원)
- [x] 캔버스 오염·콘솔 오류 0

**4. 자막 붙여넣기 (Chromium)** — 완료 2026-09-08
- [x] 토글 → textarea → 붙여넣기 → 'AI분석' 복사 → `## 자막` 섹션·5번 요청 포함, "없는 것"에서 자막 항목 제거 확인
- [x] 13,000자 입력 → 카운터 "13,000자", 프롬프트에 "앞 12,000자만 포함", 12,001자째 미포함
- [x] 링크 오버레이 위 textarea 입력 정상

**5. 프롬프트 실효성 (LLM에 실제로 붙여넣기)**
- [x] 2026-09-09 GPT 1회(시트 미첨부, 1m22s): A~F 전 절, 문장마다 [행 n]/[원칙 ID]/[가정], 세어서 말함(초간단 5/2, 모음형 0/2), "차이 없음" 인정, 좋아요율 역상관 발견, 썸네일은 [원칙]만, 권위형 세트 N 판정, Shorts Test & Compare 불가 자체 확인. **드러난 표 결함 5개**(글자수·포맷 열 없음, 태그 (없음) 오독, 중앙값 손계산, [데이터 없음] 태그 비공식) → 반영. 채점: `measure-out/…gpt-response-notes.md`
- [ ] 위 반영본으로 재실행 + **썸네일 시트 첨부** 상태에서 썸네일 근거 #번호 인용 확인
- [ ] 시장 분석 프롬프트 + 썸네일 시트를 Claude/ChatGPT에 넣어 출력 확인: 행 번호 근거를 다는지, "데이터 없음"을 지어내기로 메우지 않는지
- [ ] 자막 포함 단건 프롬프트: 대본 구조 항목이 자막 문장을 인용하는지

**6. 앱 내 LLM 분석 (`ANTHROPIC_API_KEY` 확보 후 — 돈이 든다)**
- [ ] 키 없이: `GET /api/analyze` → `enabled:false`, `POST` → 503, 버튼 잠김 (키 없이 이미 확인됨)
- [ ] 키 넣고 `GET /api/analyze` → `enabled:true, model:"claude-opus-5"`, 버튼 활성
- [ ] "앱에서 시장 분석" 1회 실행 → 결과 패널에 모델·토큰·추정 비용 표시. **실제 청구액과 추정치 대조** (가격표는 2026-06-24 캐시)
- [ ] 첨부 이미지 수가 `attachedImages`와 일치하고, 결과가 `#n` 번호로 썸네일을 지칭하는지
- [ ] 60초 안에 끝나는지. 넘기면 `LLM_TIMEOUT` — Vercel Fluid compute(300s) 또는 `LLM_EFFORT=medium`
- [ ] 폴백 발생 시 `model` 필드가 요청 모델과 다르게 오고 비용이 그 모델로 계산되는지 (또는 미지 모델이면 null)
- [ ] 단건 "앱에서 이 영상 분석" + 자막 포함 → 대본 구조가 자막을 인용하는지
- [ ] 서버 로그에 프롬프트 본문·키가 남지 않는지 (usage만)

**7. 영상 관찰 — Gemini (`GEMINI_API_KEY` 확보 후. 프리뷰 무료, 하루 8시간분)**
- [x] 키 없이: `GET /api/observe` → `enabled:false`, `POST` → 503, 버튼 잠김 (코드 경로 테스트)
- [ ] AI Studio 레이트리밋 페이지의 RPM/RPD 숫자 기록 (사용자가 페이지를 못 찾음 — 429 본문으로 대신 확인 예정. 2026-09-14 요청 7회까지 429 없음)
- [x] 영상 토큰이 usage에 세진다: 18초 1,630 · 20초 1,729 (**≈88토큰/초**, 문서의 ~100/초와 일치). 유료 티어면 과금된다고 봐야 함
- [x] 스파이크(2026-09-14): 3.8 Flash 7.8s·15.7s, 3.5 Flash-Lite 3.6s·3.3s → **동기 설계 OK**. 편당 총 2.2~2.7K 토큰 ≈ $0.004(Flash)/$0.002(Lite)
- [x] Shorts를 `watch?v=`로 넘겨도 됨(2편). 첫 문장 인용이 두 모델에서 **글자 단위로 동일** ("만원대로 치킨 배부르게 드셔보세요" @00:00 — 제목과 일치). 영상 직접 대조는 미실시
- [x] 존재하지 않는 ID → 400 `invalid_request` "Request contains an invalid argument." → `OBSERVE_BAD_REQUEST`(422). 비공개/연령제한은 미확인
- [x] **썸네일 미첨부 시 thumbnailPromise가 unknown**("썸네일 정보가 제공되지 않음") → 썸네일 이미지(hqdefault, low 해상도)를 요청에 첨부하도록 수정. 앱 경로 실측: `kept:"yes", evidence:"00:00 및 00:20에 완성된 칼집 삼겹살구이"`
- [x] 앱 경로 1편(3.8 Flash + 썸네일): 26초, 입력 2,709·출력 586 토큰 ≈ $0.0042, 보관함 저장, 단건 프롬프트에 "영상 관찰" 절 + 자막 항목 교체, 새로고침 후 "관찰 완료 1/1"·추가 요청 0
- [x] **묶음 요청**(Flash-Lite, Shorts 2편 → 1요청): 4.7초, 스키마 유효, 두 관찰 모두 순서대로, 영상 토큰 3,359(=합)·출력 825. 요청 수 절감 가능 확인
- [x] 묶음 5편 중 1편이 존재하지 않는 ID → **요청 전체 400** (부분 성공 없음). 묶음을 쓰려면 실패 시 개별 재시도가 필수
- [x] **롱폼 15:39 (P_Ors-li3Ns)**: Lite agentic 10.6s — **날조**(영어 "자연요법·배관 광고·세금 팁", 영상 토큰 0, 스키마 유효). Lite static 9.6s·영상 85,372토큰·정확("치즈 얹은 요리"). 3.8 Flash agentic 110s·정확·tool_use 14K → **agentic 폐기, 항상 static, 영상 토큰 0이면 폐기, 30분 상한**으로 코드 수정
- [x] 첫 문장 인용의 실제 영상 대조 — `PkdAiC0brvU` 첫 대사 "만원대로 치킨 배부르게 드셔보세요" **사용자 육안 확인 일치(2026-09-14)**. 두 모델 동일 + 실영상 일치로 인용 신뢰 근거 1건 확보
- [x] 병렬 3으로 20편: 429 없음(Lite). 하루 누적 ~34요청(3.8 Flash 11 + Lite 23)까지 429 없음 — "20 RPD"는 이 키엔 해당 없거나 모델별
- [x] **"영상 관찰 수집" 20편(Lite, static)**: 45초, 20/20 성공, 영상 48.2분 전송, 입력 273,090·출력 13,448 토큰, 유료 전환 시 ≈$0.12. 보관함 20건. 프롬프트 23,389자에 "20/20 관찰됨", 자막 항목 교체. 원본 `measure-out/gemini-observations-20-2026-09-14.json`, 프롬프트 `…market-prompt-with-observations.md`, 시트 PNG 4.2MB
- [x] 관찰 포함 프롬프트가 23,157자(관찰 표 47%, 구조 열이 행의 절반)라 외부 LLM이 버거워함 → 시장용 압축형(`6f32302`): 20/20 기준 **18,832자**, 관찰 절 ≈6K. 같은 세션에서 뽑은 짝: `…market-prompt-with-observations-v3.md` + `…thumbnail-sheet-v3.png` (검색 결과가 호출마다 조금 바뀌므로 프롬프트와 시트는 반드시 같은 세션 것)
- [x] **2026-09-14 GPT 2회차**(v3 + 시트 첨부, 썸네일 링크 절 삭제 후 1m36s): ① 훅 3안 `[영상관찰 #2,#3,#4,#6 00:00]`·`#8,#10` 인용 **YES**(1회차는 전부 [가정]) ② 관찰 표 세기 **NO**(첫 프레임만 씀 — 요청에 없었음) ③ 썸네일 #번호: 관찰 6절엔 씀("상위 단일 장면형 / 하위 #12·#20 콜라주 / #16 얼굴"), B절 구성 근거는 [가정/원칙]만 → **부분** ④ 체크표 YES. 정직성 좋음(숫자·질문·사회적증거 "검증용" 강등, 롱폼 n=1 일반화 거부). → 요청 7(관찰 표 세기)·B절 #번호 최소 1개·요약에 포맷별 길이/일평균 배수 중앙값·썸네일 링크 한 줄 압축으로 반영
- [x] 3회차(v4 + 시트): ChatGPT가 관찰 항목을 처리하고 **웹 검색 3회**를 하다 "연결할 수 없음"으로 **끊김**. 답변량(관찰 7 + 세트 5 × 구조 설계) 과다 + 웹 검색 지연 → 프롬프트에 **2단계 진행**(요청 1~7 답하고 멈춤 → "계속" → 시안), **웹 검색 금지**, C절 상세 구조는 1순위 세트 1개만으로 반영(`v5`)
- [x] **4회차 1단계**(v5 + 시트, 1m2s): 요청 7 세기 표 **YES** — 첫 3초 완성품 7/10 vs 5/10, 발화 0/10 vs 10/10, 약속 확인 ≤3초 "상위 아님"(원칙보다 데이터), Shorts/롱폼 분리, 요약의 포맷별 길이·일평균 배수 중앙값 사용. 2단계 진행 지시대로 멈춤. **웹 검색 금지는 무시됨(3회)** — 지시로 못 막음
- [ ] 4회차 2단계("계속"): B절 5세트에 썸네일 #번호, 훅 3안 [영상관찰]
- [x] 기본 모델 → `gemini-3.5-flash-lite` (사용자 결정 2026-09-14)
- [ ] 결정: 기본 모델 3.8 Flash → 3.5 Flash-Lite? (속도 3~4배, 비용 45%, RPD ≈500 vs ≈20, 이 표본에선 인용 동일) · 묶음 요청 옵션 도입 여부

## D. 보류 설계

| 안 | 요지 | 상태 |
|---|---|---|
| 채널 uploads로 포맷 분리 정규화 | B1 해결. 채널당 2 units, 검색 버킷과 무관. | **구현됨(`02d8bad`) — V.1·V.2로 검증** |
| 썸네일 컨택트시트 → 클립보드 이미지 | 서버 프록시 `/api/thumbnail` + 캔버스 격자 합성 + `ClipboardItem(image/png)`. 라벨이 프롬프트 표 행 번호와 일치. | **구현됨(`8fad32f`) — V.3으로 검증** |
| 자막 붙여넣기 입력란 | 0원, 법적 위험 0. 깊게 볼 1~2편용. | **구현됨 — V.4로 검증** |
| 댓글 수집 (`commentThreads.list` 1 unit) | 유일한 자연어 시청자 반응 데이터. | 검토 |
| 시간 창 표본 (`order=date` + `publishedBefore/After`) | 성과 랭킹 제거된 키워드 표본. **단**: 창 밖 relevance 집합과는 나이 분포가 달라 그대로 비교 불가, `order=date`는 스팸 편향, `publishedBefore` 무시 버그 보고([245018357](https://issuetracker.google.com/issues/245018357)) 있음. | 보류 |
| 채널 uploads로 키워드 표본 확장 | 키워드가 구체적일수록 채널 카탈로그 내 비중이 0에 수렴하고, 검색이 on-topic 영상을 이미 걷어가 잔량이 더 적다. | **철회** |
| 특징 집계를 LLM에서 코드로 이관 | n≥수백일 때 의미. 현재 n=50에서는 거짓 정밀도 위험. | 표본 확보 후 |
| LLM API 앱 내 연동 | `@anthropic-ai/sdk`, `claude-opus-5`, 썸네일 이미지 자동 첨부, 스트리밍→완성본. 키 없으면 전체 잠김. | **구현됨 — V.6으로 검증.** 스트리밍 UI·비용 캡은 미구현 |
